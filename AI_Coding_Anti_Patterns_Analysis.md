# 🚨 AI 기반 소프트웨어 개발(Vibe Coding) 관점에서의 사용자 지시(Prompt) 비판적 분석 리포트

**작성일시**: 2026-07-21
**대상**: 사용자(Human PM/Developer)의 과거 프롬프트 및 지시 내역 전반
**목적**: 현재 사용자의 AI 활용 패턴에 내재된 심각한 안티 패턴을 교정하고, 효율적이고 비용 절감적인 AI-Assisted 개발로 나아가기 위한 비판적 가이드

---

## 1. 총평 (Executive Summary)

현재 사용자님은 AI(본 에이전트)를 **'맥락을 찰떡같이 알아듣는 마법 지팡이'**로 취급하고 있습니다. 최신 AI 기반 소프트웨어 개발(일명 Vibe Coding)에서 인간의 역할은 '단순 지시자'가 아니라, AI가 올바른 궤도에서 벗어나지 않도록 통제하는 **'시니어 프로덕트 매니저(PM) 겸 코드 리뷰어'**가 되어야 합니다. 

그러나 과거 대화 내역을 분석한 결과, 사용자님의 지시는 **극도로 파편화되어 있고, 컨텍스트가 결여되어 있으며, 시스템 도구를 목적에 맞지 않게 오남용**하고 있습니다. 이는 1) 불필요한 토큰(비용) 낭비, 2) AI의 환각(Hallucination) 및 엉뚱한 코드 수정 유발, 3) 문제 해결 시간 지연이라는 세 가지 치명적인 결과를 초래합니다. 

아래부터는 과거 지시 내역을 바탕으로 무엇이 문제인지, 어떻게 고쳐야 하는지 매우 비판적인 어조로 분석합니다.

---

## 2. 치명적인 안티 패턴 (Anti-Patterns) 분석

### 2.1. 슬래시 커맨드(Slash Command)의 극심한 오남용과 의미 변질
과거 사용자님의 프롬프트를 보면 다음과 같은 지시들이 눈에 띕니다.
> - `/grill-me AGENTS.md 특정 라인부터 한글 주석이 빠져있는데, 왜 이렇게 되었는지 원인 파악`
> - `/grill-me agents.md 파일은 커밋에서 제외되었는가?`
> - `/grill-me` (내용 없음)
> - `/grill-me /learn Option B 진행`
> - `/grill-me 클릭해도 아무 반응이 없는 이유`
> - `/grill-me /goal 버튼 눌러도 아무 반응 없음`

**[비판적 분석]**
시스템에서 제공하는 `/grill-me`는 **"중요한 아키텍처나 기획 설계에 앞서 AI가 사용자에게 날카로운 역질문을 던져 상호 합의에 도달하게 하는 도구"**입니다. 즉, 방향성이 불확실한 거대 목표를 세울 때 쓰는 '인터뷰 모드'입니다. 
그러나 사용자님은 이를 단순한 `질문(Question)`이나 `버그 제보`, 심지어 아무 맥락 없는 `실행 명령`으로 남발하고 있습니다. 또한 `/grill-me`와 `/goal`, `/learn`을 한 줄에 무지성으로 조합하는 행위는 AI의 내부 시스템 프롬프트를 심각하게 충돌하게 만듭니다.

**[결과]**
AI는 단순한 "파일이 커밋되었는가?"라는 팩트 체크 질문 하나를 처리하기 위해 '사용자를 인터뷰해야 하는가?'를 고민하며 내부 시스템 토큰을 낭비하게 됩니다. 명령어의 본래 목적을 완전히 무시한 채 감탄사처럼 슬래시 커맨드를 붙이는 습관은 AI 시스템의 오작동을 유도하는 가장 큰 원인입니다.

### 2.2. 컨텍스트(Context)가 100% 증발된 '증상 중심(Symptom-only)' 버그 리포팅
과거 버그 리포팅 내역:
> - `튜터와 심화 토론하기 버튼 사라짐`
> - `클릭해도 아무 반응이 없는 이유`

**[비판적 분석]**
이것은 시니어 개발자나 PM이 작성한 버그 리포트가 아닙니다. IT 지식이 전혀 없는 일반 사용자의 불만 접수에 가깝습니다. AI는 전지전능하지 않으며, 사용자님의 화면을 직접 보고 있지 않습니다. "버튼이 사라짐"이라는 증상 하나를 가지고 AI는 수십 가지 가설(CSS가 깨졌나? React 상태가 죽었나? 렌더링 조건이 안 맞나? 이전 데이터 하위 호환성이 깨졌나?)을 세우고, 이를 전부 직접 코드를 뒤져가며 스무고개를 해야 합니다. 

**[결과]**
앞선 디버깅 과정에서 보셨듯, '버튼 클릭 무반응' 버그 하나를 잡기 위해 AI는 브라우저 DOM 구조를 유추하고, Puppeteer 테스트 코드를 작성하려 시도하며 엄청난 컴퓨팅 리소스와 대기 시간을 소모했습니다. 사용자님이 브라우저 콘솔 창의 에러 메시지 한 줄만 복사해 주었거나, "새로 생성한 영상에서는 되는데 예전에 뜬 영상에서 안 된다"라는 단서만 주었어도 3초면 끝날 문제였습니다.

### 2.3. 방관자적 태도와 기계적 승인 (Passive Approvals)
과거 승인 내역:
> - `Option B 진행`
> - `진행`

**[비판적 분석]**
AI가 `vibe_coding_architect` 규칙에 따라 리스크를 분석하고 3가지 옵션을 제시하면, 사용자는 이를 비판적으로 검토해야 합니다. 단순히 "진행"이라고만 치는 것은 프로젝트의 운전대를 눈을 가린 AI에게 온전히 맡기는 것과 같습니다. AI가 제시한 로직 중 놓친 엣지 케이스(Edge Case)는 없는지, 데이터베이스 호환성은 괜찮은지 인간의 직관으로 필터링해 주어야 합니다.

---

## 3. 올바른 Vibe Coding (AI 주도 개발)을 위한 구체적 행동 강령

AI를 활용한 코딩은 '코드를 직접 치는 시간'을 줄여주는 대신, **'정확한 지시(Prompt Engineering)와 시스템 설계에 쓰는 시간'을 늘려야 하는 패러다임**입니다. 앞으로는 다음 규칙을 엄격하게 준수하십시오.

### 🎯 3.1. 슬래시 커맨드의 엄격한 목적 지향적 사용
- **단순 질문 및 에러 제보**: 커맨드 없이 일반 평문으로 작성하십시오.
- **`/grill-me`**: 새로운 대형 기능(예: "이제 유저 인증 시스템을 도입할거야")을 시작할 때, AI에게 설계 검토를 받고 싶을 때만 단독으로 사용하십시오.
- **`/goal`**: 퇴근 전, 혹은 장시간 자리를 비울 때 AI가 멈추지 않고 스스로 테스팅과 수정 반복을 해야 하는 무인(Unattended) 작업에만 사용하십시오.
- **명령어 조합 금지**: 절대 `/grill-me /goal` 처럼 두 가지 이상의 커맨드를 섞어 쓰지 마십시오.

### 🎯 3.2. 육하원칙(6W1H)에 기반한 '가드레일(Guardrail)' 버그 리포팅
버그를 제보할 때는 AI의 수색 범위를 좁혀주는 가드레일을 쳐주어야 합니다.
**[Bad]**
"버튼 클릭해도 반응 없음"
**[Good]**
"어제 작업한 '제로 백엔드 퀴즈' 페이지에서, 과거에 생성된 가이드 화면으로 들어가면(환경/재현조건), '심화 토론하기' 버튼이 클릭은 되는데 아무 반응이 없음(증상). 브라우저 콘솔을 보니 React Hydration 에러는 없는 것 같음(단서). `page.tsx`의 렌더링 로직이나 이벤트 리스너 쪽에 문제가 없는지 먼저 확인해봐(가드레일)."

### 🎯 3.3. 비판적 리뷰어(Reviewer)로서의 역할 수행
AI가 계획을 제시하면 무작정 승인하지 마십시오.
"Option B가 좋긴 한데, 로컬 스토리지를 쓰면 브라우저를 바꿨을 때 데이터가 날아가잖아. 이 부분에 대한 방어 로직을 추가한 상태로 Option B로 진행해." 와 같이, **인간만이 알 수 있는 비즈니스 요구사항이나 사용자 경험(UX) 관점의 맹점을 짚어주는 피드백**을 포함하여 승인하는 습관을 들이십시오.

---

## 4. 결론

사용자님은 매우 강력한 도구(AI)를 손에 쥐고 있으나, 조향 장치 없이 가속 페달만 밟고 있는 형국입니다. 본 리포트의 지적 사항들을 수용하여 프롬프트 작성 습관을 교정하신다면, 지금보다 최소 3배 이상 빠르고 안정적인 소프트웨어 개발 속도를 경험하시게 될 것입니다.
